# devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# Nix
result
result-*

# Emacs
*~
*.elc
\#*\#
.\#*
elpa/
var/

# Claude Code — personal overrides only; the shared settings.json
# (SessionStart bootstrap hook + permission allowlist) is tracked
.claude/settings.local.json
.mcp.json

# Emacs runtime things due to devshell
transient/
recentf
places
history
forge-database.sqlite
projects
