# 2026-04-02
system: x86_64-linux

Launching the emacs works. Will try to work with just Vanilla emacs now.

Imported some @early-init.el settings from https://github.com/Jylhis/jotain/blob/main/early-init.el

Disabled toolbar

Enabled modus-operandi theme

Documented how to set variables

Changed `setq` to `setopt` in early-init.el for `defcustom` variables.
Added note to docs about `setopt` availability in early-init.el.

Added `transient/` to .gitignore.

Added NixOS wiki Emacs link to docs/inspiration.md.

## Tree-sitter grammars

Split the Nix build into two files:
- `emacs.nix` — bare Emacs build from source (renamed from `default.nix`)
- `default.nix` — distribution layer that wraps Emacs with all tree-sitter grammars

Used the NixOS wiki-recommended approach: `emacsPackagesFor`/`withPackages` with
`epkgs.treesit-grammars.with-grammars`. This installs all 275 tree-sitter grammars
from nixpkgs and auto-generates a `site-start.el` that sets `treesit-extra-load-path`,
so no changes to `init.el` are needed.



# x86_64-darwin

My Mac is bit slow to compile, so just to speed things up, I mached the builds to have same hash as
upstream nixpkgs for now.  This should allow me to benefit from the caching.

I updated the justfiles.

I want to setup proper language support for emacs lisp, mainly for claude code, so I started
building language.emacs-lisp options for devenv.  Currently this only installs eask and emacs-nox,
but the goal is to have LSP, formatter (treefmt), maybe some analysis tools and claude skills(?)

- https://emacs-eask.github.io
- https://github.com/elisp-lsp/ellsp
- https://github.com/emacs-elsa/Elsa


# devenv idea

Use Rust as reference https://github.com/cachix/devenv/blob/main/src/modules/languages/rust.nix

- emacs versions
- emacs-overlay
- components?
- eask, elsa as options
- similar to import, take path to directoy containing emacs package
- assertions
- extra packages to include

# extra sources for inspiration

- https://github.com/purcell/nix-emacs-ci
- https://github.com/purcell/setup-emacs
- https://github.com/purcell/package-lint
- https://github.com/laurynas-biveinis/elisp-dev-mcp
- https://github.com/marketplace/actions/emacs-lisp-check

# other

Still need to setup github actions for validation