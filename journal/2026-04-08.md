# 2026-04-08

## Performance and ergonomic settings

Applied a batch of display-performance and ergonomic settings across the config.

### Skipped (already present)
- `kill-do-not-save-duplicates` (init-core.el)
- `executable-make-buffer-file-executable-if-script-p` hook (init-core.el)
- `ffap-machine-p-known 'reject` (init-core.el)
- `winner-mode` (init-navigation.el)

### Added

**early-init.el** — bidi display optimisation (`bidi-display-reordering nil`,
`bidi-paragraph-direction 'left-to-right`, `bidi-inhibit-bpa t`).

**init-core.el** — `save-interprogram-paste-before-kill`,
`set-mark-command-repeat-pop`, `redisplay-skip-fontification-on-input`,
and save-place recenter-after-restore advice.

**init-ui.el** — `cursor-in-non-selected-windows nil`,
`highlight-nonselected-windows nil`.

**init-prog.el** — `read-process-output-max` (4 MB) before eglot block.

**init-navigation.el** — `window-combination-resize t`, reversible
`C-x 1` via `jotain-nav-toggle-delete-other-windows` (extends winner-mode).
