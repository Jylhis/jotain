# Naming conventions:
# - .bash files: Bash scripts (use bash-specific features)
# - .sh files: POSIX sh scripts (portable, no bashisms)

# Default shell for .sh files
shell=sh

# Enable all optional checks
enable=all

# Disable specific checks globally (if needed)
# disable=SC2034  # Unused variables

# Source paths for finding sourced files
source-path=SCRIPTDIR
