;;; editor/packages.el --- Editor packages -*- lexical-binding: t; -*-
;;; Commentary:
;; Package declarations for core editor functionality.
;; Packages are installed via Nix (default.nix), this file documents dependencies.
;;; Code:

;; Core editing packages
;; super-save       - Auto-save on focus loss
;; vundo            - Visual undo tree
;; expand-region    - Expand selection semantically
;; multiple-cursors - Multiple cursors editing
;; drag-stuff       - Drag lines/regions up/down
;; pretty-sha-path  - Pretty SHA paths display

;;; packages.el ends here
